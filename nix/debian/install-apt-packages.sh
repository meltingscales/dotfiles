#!/bin/bash

sudo apt install snap tmux lynx wget curl htop docker python3-pip gnupg
# virtualbox for debian 11? QwQ

# docker?
# see https://docs.docker.com/engine/install/debian/


sudo snap install --classic powershell
sudo snap install           discord